# Author headshot

Client confirmed 2026-08-13: the "About Marcia" full-bleed band on the
homepage (`index.md`) and the About page's headshot slot
(`about.md`) should both use a current portrait headshot of
Marcia, referred to as "marcia-headshot" — resolving the earlier open
question about whether that homepage band should be a photo or a
background/textured image.

## Committed

- `marcia-headshot.jpg` — real photo, in use on both `index.md` (`.about-teaser__photo`) and `about.md` (`.about-split__photo`).
- `marcia-headshot.webp` — WebP copy served via `<picture>`/`<source>` ahead of the `.jpg` fallback on both pages.

Nothing pending here — delete this README once no longer useful as a reference.
